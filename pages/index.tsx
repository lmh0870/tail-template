import ReactionContainer from "@components/ReactionContainer";
import { NextPage } from "next";

const Page: NextPage = () => {
  return (
    <div>
      <ReactionContainer />
    </div>
  );
};

export default Page;
